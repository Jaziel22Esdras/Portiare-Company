{
  "name": "portiare-company-site",
  "version": "1.0.0",
  "description": "Site institucional Portiare Company",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx netlify deploy --dir=dist",
    "deploy:prod": "npx netlify deploy --dir=dist --prod"
  },
  "devDependencies": {
    "netlify-cli": "^25.5.0",
    "vite": "^5.0.0"
  }
}
